task strings {

    println "Benoit".concat(" Liessens").toUpperCase()
}


task md5 << {
    def byteArray = java.security.MessageDigest.getInstance('MD5').digest("BenoitLiessens".getBytes())
    byteArray.each { c ->
        print Integer.toHexString(0xFF & c)
    }
}

task jvm << {
    println "Using JVM " + System.getProperty("java.version")
}

